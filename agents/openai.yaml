interface:
  display_name: "Project Delivery Analyst"
  short_description: "项目理解、PRD、AI工作流、开发门禁与合规交付"
  default_prompt: "Use $project-delivery-analyst to classify the project/task, confirm scope, route to the correct references for PRD, project understanding, AI workflow/batch gates, OpenSpec/Loops, development/fix, release, and task trace, then produce evidence-labeled deliverables without expanding scope."
policy:
  allow_implicit_invocation: true
